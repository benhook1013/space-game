#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/bootstrap_flutter.sh"
if [[ "${1-}" == "format" && $# -eq 1 ]]; then
  set -- "$1" .
fi
echo "[dartw] dart $@"
exec ".tooling/flutter/bin/dart" "$@"
